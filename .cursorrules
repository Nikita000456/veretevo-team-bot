# 🎯 Правила разработки для проекта VeretevoTeam

## 📋 Общие принципы

### 🚨 КРИТИЧЕСКИ ВАЖНО
- **Новый функционал НЕ должен ломать предыдущий функционал**
- **Всегда создавать резервные копии критических файлов перед изменениями**
- **Тестировать каждый шаг изменений и проверять импорты модулей**
- **Писать на русском языке** (комментарии, логи, сообщения пользователям)
- **Мониторить логи после изменений** для выявления ошибок
- **Проверять синтаксис** перед перезапуском сервиса
- **Перезагружать бота ТОЛЬКО через скрипт `./scripts/restart-bot.sh`** - это основной и единственный скрипт перезагрузки
- **УДАЛЕНЫ все другие скрипты перезагрузки** - оставлен только `restart-bot.sh`

### 🎯 Приоритеты разработки
1. **Безопасность** - резервные копии и проверка синтаксиса
2. **Совместимость** - не ломать существующий функционал
3. **Тестирование** - проверка каждого шага
4. **Документация** - обновление README для новых функций
5. **Очистка** - удаление временных файлов
6. **Контроль версий** - всегда коммитить изменения перед перезагрузкой

## 🏗️ Архитектура проекта

### Структура папок
```
VeretevoTeam/
├── main.py                          # Главный файл бота
├── handlers_veretevo/               # Обработчики команд
├── services_veretevo/               # Бизнес-логика
├── utils_veretevo/                  # Утилиты
├── config_veretevo/                 # Конфигурация
├── data/                            # Данные (tasks.json, answers.json)
├── logs/                            # Логи
├── scripts/                         # Скрипты управления
├── tests/                           # Тесты
└── docs/                            # Документация
```

### Ключевые файлы
- `main.py` - точка входа
- `handlers_veretevo/menu.py` - главное меню
- `handlers_veretevo/tasks.py` - управление задачами
- `services_veretevo/task_service.py` - логика задач
- `config_veretevo/env.py` - переменные окружения
- `data/tasks.json` - база задач
- `data/answers.json` - база знаний GPT

## 🔧 Стандарты кодирования

### Python код
- **Использовать Python 3.x**
- **Следовать PEP 8** для форматирования
- **Добавлять типизацию** где возможно
- **Писать docstrings** для функций
- **Использовать логирование** через `logging`

### Импорты
```python
# Стандартная библиотека
import logging
import json
from datetime import datetime

# Сторонние библиотеки
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup
from telegram.ext import ContextTypes

# Локальные модули
from handlers_veretevo.menu import show_main_menu
from services_veretevo.task_service import create_task
from utils_veretevo.keyboards import get_task_keyboard
```

### Логирование
```python
import logging
logger = logging.getLogger(__name__)

# Использовать уровни логирования
logger.info("✅ Успешное действие")
logger.warning("⚠️ Предупреждение")
logger.error("❌ Ошибка")
logger.debug("🔍 Отладочная информация")
```

## 🎮 Логика меню и кнопок

### Главное меню (только в личных чатах)
```python
main_menu_keyboard = [
    ['📌 Новая задача'],
    ['📋 Список задач'],
    ['Мои задачи']
]
```

### Правила кнопок
- **Главное меню** показывается только в личных чатах (private)
- **В группах** - только текстовые сообщения, без меню
- **Кнопка "❌ Отмена"** всегда отдельной клавиатурой
- **Inline-клавиатуры** для выбора отделов

### Ограничения доступа
- **GPT функционал** - только для директора (ID: 406325177)
- **Административные команды** - только для директора
- **Обычные пользователи** - полный доступ к задачам своего отдела

## 📊 Работа с данными

### JSON файлы
- `data/tasks.json` - структура задач
- `data/answers.json` - база знаний GPT
- `config_veretevo/departments_config.json` - конфигурация отделов

### Структура задачи
```json
{
  "id": "unique_id",
  "text": "Текст задачи",
  "department": "assistants",
  "author_id": 123456789,
  "assignee_id": 987654321,
  "status": "new",
  "created_at": "2024-01-01T12:00:00",
  "media": []
}
```

### Статусы задач
- `new` - новая
- `in_progress` - в работе
- `completed` - завершено
- `cancelled` - отменено

## 🔄 Процесс внесения изменений

### 1. Анализ и планирование
- [ ] Изучить текущую структуру кода
- [ ] Определить точку входа для изменений
- [ ] Спланировать последовательность действий
- [ ] Оценить влияние на другие модули

### 2. Предварительная проверка
- [ ] Проверить синтаксис существующих файлов
- [ ] Убедиться в корректности импортов
- [ ] Создать резервные копии критических файлов

### 3. Внесение изменений
- [ ] Вносить изменения пошагово
- [ ] После каждого изменения проверять синтаксис
- [ ] Тестировать импорты модулей

### 4. Тестирование и валидация
- [ ] Проверить компиляцию всех измененных файлов
- [ ] Протестировать импорты ключевых модулей
- [ ] Создать временные тестовые файлы при необходимости
- [ ] Удалить временные файлы после проверки
- [ ] Проверить логи на наличие ошибок

### 5. Документация
- [ ] Обновить README.md с описанием новой функциональности
- [ ] Добавить инструкции по настройке
- [ ] Включить примеры использования

### 6. Финальная проверка и развертывание
- [ ] Проверить статус сервиса
- [ ] Перезапустить бота: `./scripts/restart-bot.sh "Описание изменений"` (основной скрипт)
- [ ] Проверить статус после перезапуска
- [ ] Убедиться в корректной работе
- [ ] Мониторить логи на наличие ошибок

### 7. Очистка
- [ ] Удалить временные тестовые файлы
- [ ] Очистить кэш Python (`__pycache__`)
- [ ] Проверить логи на наличие ошибок

## 🛠️ Команды для разработки

### Проверка синтаксиса
```bash
python3 -m py_compile <файл>
```

### Проверка импортов
```bash
python3 -c "from <модуль> import <функция>; print('✅ OK')"
```

### Управление сервисом
```bash
# ПЕРЕЗАГРУЗКА БОТА (основной скрипт)
./scripts/restart-bot.sh "Описание изменений"

# Проверка статуса
sudo systemctl status veretevo-bot

# Просмотр логов
journalctl -u veretevo-bot -f
```

### Резервное копирование
```bash
# Создание резервной копии
cp <файл> <файл>.backup.$(date +%Y%m%d_%H%M%S)

# Восстановление из резервной копии
cp <файл>.backup.* <файл>
```

### Очистка кэша
```bash
find . -name "__pycache__" -type d -exec rm -rf {} + 2>/dev/null || true
```

## 🚨 Экстренные процедуры

### Если бот не запускается:
```bash
# Проверить логи
journalctl -u veretevo-bot -n 50

# Восстановить из резервной копии
cp <файл>.backup.* <файл>

# Перезапустить сервис (только в экстренных случаях!)
sudo systemctl restart veretevo-bot

# В обычных случаях использовать:
# ./scripts/restart-bot.sh "Восстановление после сбоя"
```

### Если есть ошибки синтаксиса:
```bash
# Проверить конкретный файл
python3 -m py_compile <файл>

# Исправить ошибки и повторить проверку
```

## 🎯 Специфичные правила проекта

### Telegram Bot API
- **Использовать python-telegram-bot** версии 20+
- **Обрабатывать все типы сообщений** (текст, фото, документы, голос)
- **Поддерживать inline-клавиатуры** для выбора отделов
- **Логировать все действия** пользователей

### Обработка голосовых сообщений
- **Интеграция с Yandex SpeechKit** для распознавания речи
- **Автоматическое создание задач** из голосовых сообщений
- **Определение отдела** по ключевым словам

### GPT интеграция
- **Yandex GPT API** для генерации ответов
- **База знаний** в `data/answers.json`
- **Доступ только для директора** в групповых чатах
- **Умные кнопки** (Да/Нет) для быстрых ответов

### Мониторинг групп
- **Автоматическая синхронизация** участников отделов
- **Логирование изменений** в группах
- **Тихий режим** для продакшена (без уведомлений)

### Отчеты
- **Утренние отчеты** в 7:30
- **Вечерние отчеты** в 18:00
- **Автоматическая отправка** в соответствующие чаты

## 🚫 Запрещенные действия

### ❌ НЕ делать:
- **Хардкодить test_mode** - использовать аргументы функций
- **Модифицировать @VeretevoTeam** - создавать новые боты в отдельных папках
- **Использовать английские имена** в базе данных
- **Отправлять только уведомления** - предпочитать summary emails
- **Писать подробные отчеты** - пользователь предпочитает краткие сводки
- **Показывать меню в группах** - только в личных чатах
- **Использовать кнопку "Помощь"** - полностью убрана
- **Перезагружать бота вручную** - всегда использовать скрипт `./scripts/restart-bot.sh`

### ✅ Рекомендуется:
- **Создавать резервные копии** перед изменениями
- **Тестировать пошагово** каждый этап
- **Обновлять документацию** для новых функций
- **Использовать русские названия** в интерфейсе
- **Логировать все изменения** в audit.log
- **Перезагружать бота через `./scripts/restart-bot.sh`** - автоматический коммит в GitHub + перезапуск

## 📚 Документация

### Обязательные файлы для обновления
- `README.md` - описание новой функциональности
- `FUNCTIONS_AVAILABLE.md` - список доступных функций
- `docs/standards/DEVELOPMENT_STANDARDS.md` - стандарты разработки
- Соответствующие файлы в `docs/guides/` для новых функций

### Структура документации
- **README.md** - общий обзор проекта
- **docs/guides/** - руководства по конкретным функциям
- **docs/standards/** - стандарты разработки
- **FUNCTIONS_AVAILABLE.md** - описание доступных функций

## 🎯 Приоритеты при разработке

1. **Совместимость** - не ломать существующий функционал
2. **Безопасность** - резервные копии критических файлов и проверки синтаксиса
3. **Тестирование** - проверка каждого шага и импортов модулей
4. **Документация** - обновление README с примерами использования
5. **Очистка** - удаление временных файлов и кэша Python
6. **Мониторинг** - проверка логов после изменений и статуса сервиса

---

*Правила обновлены: 2024-01-01*
*Статус: ✅ Активные правила для разработки* 